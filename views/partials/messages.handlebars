  <article class="messages">
    <div class="title">
      {{messages}}
    </div>
    <div class="meta">
       by {{user.username}} on
      {{format_date created_at}}
      
      {{!-- <a href="/userMessages/{{id}}">{{comments.length}} {{format_plural "comment" comments.length}}</a> --}}
    </div>
  </article>
